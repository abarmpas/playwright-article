export const testConfig = {
    kafka: {
        KAFKA_HOST: `${process.env.KAFKA_HOST}:9092`,
        KAFKA_USERNAME: `${process.env.KAFKA_USERNAME}`,
        KAFKA_PASSWORD: `${process.env.KAFKA_PASSWORD}`,
    },
};